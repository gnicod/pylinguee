from setuptools import setup

setup(name='pylinguee',
      version='0.1',
      description='wip linguee wrapper',
      url='http://github.com/gnicod/pylinguee',
      license='MIT',
      packages=['pylinguee'],
      install_requires=[
          'bs4',
      ],
      zip_safe=False)
